import { defaultPatternMap } from "@/src/patterns/patterns";
import { defaultEffectMap } from "@/src/effects/effects";

export const defaultPatternEffectMap = {
  ...defaultPatternMap,
  ...defaultEffectMap,
};
