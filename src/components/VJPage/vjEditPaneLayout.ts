/**
 * Chakra spacing token for `ml` on VJ edit pane: section headings + their controls,
 * and pattern/effect parameter cards — keeps left edges aligned.
 */
export const VJ_EDIT_PANE_CONTENT_ML = 4;
